# 035/036 项目空间 UI 增量验收

日期：2026-09-26。两批分别修项目详情信息披露和项目首页密度，目标是接近 WorkBuddy 5.6.2 实机所见的信息层级；**不核销 PS-01–11 的真实多用户旅程，也不宣称功能或视觉 1:1**。

| 批次 | GLM 固定审查提交 | 合入并推送 `xiongbao/main` | 已交付范围 |
| --- | --- | --- | --- |
| 035 项目详情 | `9abbb7581a6c2dea1974b8df271c0bd6f943172b` | `089a484d022a36ad0ca77a8b2bb5b9ca70f47592` | 页面级隐藏 h1；专家、成员分卡各保留一个命名区域；项目信息披露支持 Escape、外点和切项目关闭，长描述在短窗内独立滚动；移动端面板定位到完整页头，不再从左侧出屏。 |
| 036 项目首页 | `d68583706585d5dad0491cad8347dd56a0800469` | `6e0f085af6acb8656a5b45a41d571d9d239dde03` | 去掉 PageShell 大外框；真实 API 项目卡缩至约 78px，保留角色与服务端更新时间；五个模板入口在 1273×634 首屏可见，真实列表、搜索、邀请、新建流程继续沿用。 |

两次 GLM-5.3 只读固定差异审查均为 GO、无 P0/P1，GLM 没有运行测试。035 的 Claude/Qwen 候选因执行环境拒绝 shell 命令，无法自行跑测试；Codex 独立测试并在浏览器发现 390px 弹层左侧越界，随后只在所分配的样式文件中修正、格式化并再次测试。035 隔离工作树的 ProjectDetail 定向测试为 25/25、Projects 模块为 207/207；036 的 ProjectSpace 定向测试为 17/17、Projects 模块为 208/208。两批合入后，Windows 主线 Projects 模块 10 文件 **213/213**，生产构建（含 TypeScript）退出码 0。样式与 TSX 的目标 ESLint/Prettier、差异检查通过；构建的大 chunk 与 jsdom 伪元素测量警告没有使命令失败。WSL 候选提交的 hook 因 CRLF shebang 无法启动，Codex 在手工检查通过后仅对该次提交绕过 hook；不把它记为 hook 已通过。

合成 API 的 Chrome 验证覆盖 1273×634、1280×768、800×728 和 390×844。035 四标签共用一个禁用输入框，右侧配置独立滚动；45 行合成长描述在披露层内可滚动，390px 弹层边界实测 x=12–378px。036 首页六个合成项目在 1273px 三列、800px 两列、390px 单列；卡高 78px，1273×634 首个模板卡 y=375.6px，搜索/模板/通用新建路径和页面横向溢出检查通过。**800px 浏览器检查手动预收起全局导航**；032 自动收起仍未验收。合成服务、测试账号和截图不等同于用户真实多成员或 WorkBuddy 的写入验收。

剩余 UI 与功能差距：项目详情 composer 仍禁用；本地/云端任务模式、工作空间与权限控制缺真实安全执行链；连接器、技能和定时任务卡未开放。035 审查留下的 P2 为面板外 Escape 可能抢焦点、`infoPanel` 英文无障碍标签缺 locale 键、非 rose 深色主题小字对比度不足，以及移动端滚动时面板随页头移出；036 的 P2 是 700–767px 间距断点不一致与既有桌面 no-drag 区文本选择问题。模板名称留空与草稿保护另由 037 单独审查，不计入本记录。详细功能差距见 [项目空间台账](PROJECT_SPACE_GAP_AUDIT.md)。

后续 [038 固定提交验收](PROJECT_DETAIL_038_ACCESSIBILITY_ACCEPTANCE.md)已关闭上述前三项 035 P2；移动端滚动仍开放。本段保留 035/036 当时审查的原始问题记录。
