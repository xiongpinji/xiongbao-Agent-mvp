# 030A B2 受控文件运行时验收

2026-09-26。B2 从已推送的 M0/B1 基线实现项目文件任务的专用内部 Agent 生命周期。Claude/Qwen 留下四处失败补偿与重启缺陷，OpenCode/DeepSeek 的第二次承接未改动代码；经用户授权，Codex 补了先红后绿的回归并修复。固定候选 `4813fa1d5ef07ed93b1fdaec06895d2da4776b0e` 获 GLM-5.3 **只读 GO，无 P0/P1**；主线集成提交为 `6777b02662019e1fe3dfe0dbe24fa6aca21bebd0`。

内部运行时走 B1 原子配额登记、独立托管目录、同步启动与严格的六工具虚拟文件系统校验。源专家的 backend、连接器、技能、插件、ACP、知识与工作区字节不被复制。创建失败时依次清理 harness、私有根和数据库；无法确认清理完成就留下 `failed` 记录供重试。Codex 修复了数据库删除失败后残留 `running`、年轻失败/无线程记录重启、锁等待期间内部标记变化仍被清理，以及审计/终读失败后残留活运行时。普通 Agent 和有私有线程的内部运行时不进入无引用清理。

Codex 在隔离候选先观测五项新增回归失败，修复后六项焦点测试通过；B2/B1/相邻项目定向 **233 通过、3 跳过**，五个变更文件 Ruff check/format、manager/paths 严格 mypy 和差异检查通过。合入后在 WSL 主线同组 **233 通过、3 跳过**；Windows 主线运行时、恢复与路径定向 **63 通过、5 跳过**，其中跳过项受平台条件限制。GLM 没有运行测试；未把旁路 `.b2_ruff_runner.py` 纳入提交。

GLM 留下一个 P2：严格启动校验没有逐一断言配额/项目指令中间件与部分配置字段仍存在。B3 必须事务化处理线程建立与无引用清理的竞态，B3/B4 必须阻断通用 Agent 删除、枚举、管理和聊天/文件旁路，完整删除只能走本人专用流程。**`mode="files"` 仍关闭**；B2 验收不等于真实项目任务可用、完整本地 shell、云端协作、WorkBuddy 视觉或 PS-08 旅程验收。
