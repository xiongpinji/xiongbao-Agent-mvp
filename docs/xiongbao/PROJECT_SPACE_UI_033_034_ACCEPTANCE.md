# 033/034 项目空间 UI 增量验收

日期：2026-09-26。目标是以熊宝品牌接近用户已打开的 WorkBuddy 5.6.2 项目空间信息层级，保持已有真实项目 API、成员权限和禁用能力边界。两批均为 UI 增量，**不核销 PS-01–11 的完整用户旅程，也不宣称功能或视觉 1:1**。

| 批次 | 固定审查提交 | 合入并推送用户 `xiongbao/main` | 已交付范围 |
| --- | --- | --- | --- |
| 033 详情工作台 | `f31c868c2033068a41e61a3c35a56b87adc41cc6` | `c77fb9e9e356bb219e1ffc35c08873f367d309e1` | 紧凑面包屑/动作头、键盘可操作项目信息披露、中央四标签下单一禁用输入框、右侧 300px 独立滚动配置分卡。项目指令可预览/展开，真实专家和成员组件保留，未实现项明确标为未开放。 |
| 034 项目首页 | `f2f0342641118920d33999faafe60efe1c507034` | `5690feabfc52724019bbb9f8720840bc890ca091` | 熊宝品牌介绍、“我的项目”与右侧搜索、真实 API 项目网格、五个模板入口接现有创建弹窗；通用新建仍是空表单，模板只预填且不会自动创建。 |

GLM-5.3 只读审查分别对 033、034 固定差异给出 GO，无 P0/P1；GLM 只读任务没有运行测试或读取 WorkBuddy 私有项目数据。033 的“标题栏按钮可能被拖拽吞掉”疑点已由 Codex 检查 `dashboard/src/styles/layout.css` 的按钮/链接 `no-drag` 规则排除。其余详情披露层裁剪、关闭方式、标题与卡片语义等 P2 进入后继修整。034 的“弹窗开启时程序化更换模板会重置草稿”为当前 UI 不可达的 P2；中英文既有模板键由 Codex 在仓库核对存在。

Codex 对隔离候选分别运行了 033 项目测试 **201/201**、034 项目测试 **195/195**，并完成 TypeScript、目标 ESLint/Prettier、差异检查、生产构建及合成 API 浏览器检查。合入两批后的 Windows 项目模块回归为 **10 文件、206/206 通过**；`npm run build`（`tsc -b && vite build`）退出码 0。测试过程中 jsdom 报告不支持伪元素 `getComputedStyle`，构建提示动态/静态导入和大 chunk；这些是非零日志警告，未导致测试或构建退出失败。WSL 的 pre-commit hook 因 CRLF shebang 无法启动，隔离候选提交只在手工检查通过后以单次 hook bypass 完成；这不是 hook 已通过的证据。

隔离 Chrome 的 033 检查覆盖 1273×634、1280×768、800×728 和 390×844 的四标签、禁用输入框数量、配置区独立滚动与水平溢出。034 用六个合成项目覆盖相同四尺寸、真实列表/搜索请求、模板预填及通用空白创建；1273px 为三列、800px 为两列、390px 为单列，没有页面级水平溢出。**800px 检查手动预收起全局导航为 56px rail**；032 自动收起仍是未验收独立项。上述浏览器检查用合成数据和测试服务，不替代已登录生产账号及多成员真实旅程。

与 WorkBuddy 的显著差距仍在：首页还有 PageShell 大外框，项目卡约 121px 高，模板标题在 1273×634 首屏底部；WorkBuddy 实机是更紧凑的首页。其模板弹窗名称留空并有资源选择，熊宝仍预填名称/描述/指令。项目详情的输入框不能直接发送，缺少真实本地/云端任务运行、工作空间和权限控制；连接器、技能、定时任务没有项目级执行链。项目任务协同、资产引用/回收/Office 预览、通知、外部数据源和真实 PostgreSQL/Windows 安装验收仍在 [项目空间台账](PROJECT_SPACE_GAP_AUDIT.md)。后继 035/036 分别修详情披露层与首页密度，在审查、验证和推送前不计为已交付。
