# 040 项目模板弹窗状态与错误提示验收

2026-09-26。040 在 039 已推送基线 `4909619d` 上处理两项可复现的 P2：父项目弹窗关闭后，未保存内容的覆盖确认仍可能留在页面；无效项目名称的动态错误缺少辅助技术提示。OpenCode/DeepSeek 只改 `CreateProjectModal.tsx` 与 `ProjectSpace.test.tsx`，先验证确认框和错误提示回归在旧代码上失败，再实现确认句柄的关闭、卸载、编辑目标切换清理，以及无效名称的 `role="alert"`。同一项目资料刷新仍保留草稿，旧确认回调不能写入后续打开的草稿。

GLM-5.3 对固定候选 `d373a58c81f2c627568125e4d79720e5896b48a1` 给出**只读 GO、无 P0/P1**。它提出的“组件重挂载导致旧会话令牌碰撞”经真实旧回调探针在修复前即为绿：旧 React 实例的状态设置不会改动新实例，因此没有增加全局令牌。Codex 核对干净的固定工作树与两文件差异，并独立在 WSL 候选运行 Projects **229/229**、目标 ESLint、按 CRLF 规则的 Prettier、`tsc -b`、Vite build 和差异检查；合入主线为 `01e1ecfd` 后，Windows Projects **229/229**、`tsc -b`、Vite build 也通过。构建仍有既有的混合导入与大 chunk 警告。GLM 未运行测试。

GLM 留下三项非阻塞 P2：中性帮助节点被 React 复用为 `role="alert"` 时，首次错误在部分屏幕阅读器上可能不播报；编辑目标切换和卸载路径目前仅以确认句柄 mock 验证，真实 antd 弹窗移除的行为测试只覆盖父弹窗关闭；原有静态 `Modal.confirm` 绕过应用主题绑定。需要分别补真实播报/节点插入、弹窗行为测试和主题绑定，再做桌面辅助技术验收。040 不开放项目文件任务或项目配置能力，也不核销 PS-01 完整多用户旅程与 WorkBuddy 逐状态视觉对照；项目空间双重验收仍为 **0/11**。
