# Goal

以 [PROJECT_SPACE_SPEC.md](PROJECT_SPACE_SPEC.md) 为合同，将熊宝-Agent 项目空间做成真实的多人协作域，逐项达到 WorkBuddy 5.5.6 已观察和官方记录的项目行为；完成可验证的服务端授权、前端旅程、独立只读审查和持续推送。

# Decisions and assumptions

- Codex 总指挥；`claude-bailian/qwen3.8-max` 是后端主实现；`opencode-bailian/bailian-token-plan-personal/deepseek-v4.1-flash` 是互不重叠的前端并行实现；`qwen-code-review/glm-5.3` 只读审查。每位实施者在独立 worktree 工作，不创建子代理，不提交/推送/部署/改凭据/清理用户数据。
- 首片仅做真实项目 CRUD、owner 成员记录和 ACL。表格/看板、资产、项目任务与配置在有实际数据源前保持诚实的待建设状态；绝不用专家团 `teams`、RAG 资料库或 Agent 工作区伪装项目空间。

# Constraints and guardrails

- 项目成员资格不授予任何私密任务正文或本地文件权限。所有服务端入口做成员/资源授权，包括搜索、下载、深链、事件与 WebSocket。首片 API 和角色见规格文档。
- 共享接线文件 `api/app.py`、`infra/db/services.py`、`dashboard/src/routes/index.tsx`、`dashboard/src/layouts/sidebarNav.tsx` 分属各片明示的唯一所有者；主仓库只由 Codex 整合。已推送项目基座 018、成员 019、计划 020；021 已分配给[私密项目任务关联](PROJECT_TASK_ACL_CONTRACT.md)。任务工作目录须另定后续迁移版本，不覆盖 021，避免已有用户升级时跳过或重用版本。
- 先写行为测试再实现，SQLite/PostgreSQL 都验证；不能将 UI 展示、源码存在、单测、实机旅程或 1:1 验收混为一谈。外部登录/邮件/云任务等没有真实授权的环节以可控本地样例验证，保留真实验收缺口。

# Checklist

- [x] 001 · Claude 主线：018 双数据库迁移、项目 repo/service、`GET/POST/GET:id/PATCH` 和成员列表 API，事务内 owner/事件，非成员与角色隔离测试。只拥有后端项目新文件和明确接线文件。
- [ ] 002 · OpenCode 并行线：按规格中固定 API 合同实现项目左栏入口、项目搜索/创建/详情四页签布局、成员与项目配置只读真实展示/未支持提示、加载/空/错误态与测试。只拥有前端项目文件和明确接线文件，不写后端。
- [x] 003 · GLM 只读：对 001/002 合并快照核查越权、404/403、SQL 兼容、UI 假能力及 WorkBuddy 行为差距，列 P0/P1 和验收样例。
- [x] 004 · Claude 主线：成员邀请/申请/审批、角色变更和撤权，安全令牌、审计、双用户权限测试。依赖 001。
- [x] 005 · OpenCode 并行线：计划待办真实 CRUD/流转/指派、表格与看板、筛选和刷新一致性；需要独立后端契约与分工，依赖 001。020 代码片与定向/隔离浏览器关键链路已验；PG 实库与 WorkBuddy 视觉核销仍在 PS-04 差距台账中。
- [ ] 006 · Claude 主线：项目任务关联、私密默认、显式共享/协同/移交及历史/文件/流权限；项目配置快照和连接器凭据隔离。依赖 004 和任务工作目录安全方案。已拆为 [021 私密任务关联](PROJECT_TASK_ACL_CONTRACT.md) → PS-05B 显式共享及全入口 ACL → PS-05C 协同/移交与配置快照；第一片不得宣称整个 006 完成。
- [ ] 007 · OpenCode 并行线：项目资产库与版本、添加到任务、容量和权限；从独立真实存储模型起步，不复用未授权的本地路径。依赖 004/006，必要时拆片。
- [ ] 008 · Codex + GLM：本地/云端模式、动态/审计、双用户与非成员 E2E、1280×768 逐页 UI 对照、定向/全量测试、Windows 构建、差距矩阵核销、批次推送。依赖各已实施片。

## 项目空间后继实施队列

以下把 006–008 的大项拆成可单独验收的批次；**排队不等于启动或完成**。每批在实施前固定 API/ACL 合同与不重叠文件白名单，在相应依赖通过后派给同一已授权三路由。Claude 负责后端权限/迁移，OpenCode 负责前端交互，GLM 只读审查，Codex 负责集成、浏览器旅程与推送。除 021 外，迁移编号在前批实际合入后分配，不预占或重用。

| 批次 | 所属旅程与交付边界 | 依赖 | 可核销的最小证据 |
| --- | --- | --- | --- |
| PS-05A | 021：本人既有 Dashboard 私聊任务归属、个人列表、解绑；默认私密 | 018/019 项目成员 | 本人关联并刷新仍在；他人/非成员列表、详情、旧深链无法枚举 ID/标题；移除成员后关联清零，原历史仍归本人。代码、三身份浏览器关键链路与 GLM 只读审查已完成并推送 `351c4e64`；PG 实库和完整视觉仍待验。 |
| PS-03A | 022：项目与成员、待办安全事件分页，纯文本留言创建/读取；“与我相关/成员动态”筛选。邀请/加入申请事件不进入动态 | 018–021；遵守 021 私密事件禁写合同 | `aeb8e01d` 实现、`6e4a216e` 修复 GLM P2；Codex 复测 132 后端、73 前端用例及三身份浏览器关键链路通过。GLM 对固定首提交只读审查无 P0/P1；PG 实库和 WorkBuddy 视觉仍待验。任何事件均不得含私密任务 ID、凭据或项目指令正文。 |
| PS-06A | 023A：独立私有资产根、文件夹、上传、列表/搜索/类型/容量和成员下载；不可变首版记录，合同见 [项目资产合同](PROJECT_ASSET_CONTRACT.md) | 019 成员 ACL、022 已合入、独立存储与失败回收设计 | 首提交 `a58d7b36` 经 GLM 源码只读审查 GO、无 P0/P1；Codex 已修复可执行 P2，资产定向 99 通过、5 项 Windows POSIX 专项跳过，三身份浏览器关键链路及 WSL 私有权限/符号链接探针通过。PG 实库与 WorkBuddy 视觉未验；023B 版本/恢复/删除另批。 |
| PS-06B | 023B：版本历史/恢复、同名文件新版本、删除/恢复、回收与项目配额 | PS-06A 真实存储与成员 ACL | 旧版本每次下载仍校验成员；撤权/删除使旧链接失效；恢复字节与版本记录一致，回收与容量有故障注入证据。 |
| PS-06B-1 | 026：同一文件的不可变版本列表、新版本上传、旧版成员下载、当前版恢复及项目资产弹窗 | PS-06A、019 成员 ACL；合同见 [026 版本合同](PROJECT_ASSET_VERSIONS_CONTRACT.md) | GLM 对固定代码 `55dc6d9b` 与后续六文件补丁 `594b41d4` 均只读 GO、无 P0/P1；Codex 修复首轮日志 P2 后，Windows 资产单元 83 通过/5 平台跳过、WSL 88 通过。API 集成 Windows 25 在修复后通过，WSL 25 在修复前通过、修复后未重跑。隔离 Chrome 三身份走通上传、历史字节、恢复、跨节点 404 和撤权；远端 `main` 已核对为 `594b41d4`。其余前端 P2、PG 实库及完整视觉仍独立跟踪；本片不含预览、删除/回收、配额和任务引用。 |
| PS-06B-1P | 027：成员登录态 PDF 历史版预览、请求取消与回收日志私有路径修复；合同见 [027 预览合同](PROJECT_ASSET_PREVIEW_027_CONTRACT.md)、[本批验收](PROJECT_ASSET_PREVIEW_027_ACCEPTANCE.md) | 026 历史版本下载和弹窗；安全取字节与本地渲染 | 设计经 GLM 条件 GO 后收紧 404 与晚到响应合同；Claude 与 OpenCode 的精确白名单产物已由 Codex 整合。Windows 全仓非 live 3985 通过/123 跳过、受影响前端 83/83、类型/格式/生产构建及隔离三身份浏览器通过。固定代码 `699c709f` 经 GLM 只读 GO、无新增 P0/P1，已快进推送并核对远端 SHA。PDF 大纲外链协议白名单为待修 P2；仅 PDF 预览，不核销 Office、删除/回收、配额、任务引用或 PS-06B 整体。 |
| PS-04B | 待办详情富文本、评论及图片粘贴上传、动态回显；020 原 CRUD 保持兼容 | 020；图片存储与授权依赖 PS-06A 的可复用组件，或单独设计受控评论附件 | 富文本安全渲染与粘贴图片失败/重试；评论按成员授权，旧纯文本待办无损打开，图片旧链接撤权即失效。 |
| PS-05B-1 | 024：任务本人向同项目指定成员显式授予/撤回**卡片摘要**只读权限；`own/shared/all` 三范围，不解锁正文、附件、流 | PS-05A、019 成员 ACL；合同见 [024 卡片分享](PROJECT_TASK_SHARE_CONTRACT.md) | `e98cea4f` 已推送：后端项目域回归 312 通过、4 项 Windows POSIX 专项跳过，前端项目页 118/118，三身份浏览器授权/撤回/成员移除通过；GLM 固定快照只读审查 GO、无 P0/P1。SQLite 双连接交错写已补测并独立重复 5 次；PG 实库、WorkBuddy 分享面板和完整视觉不在此片验收。 |
| PS-05B-2A | 025：有效 024 卡片接收者经任务本人**再次明确确认**后，仅在项目内只读查看已投影的 human/assistant 文本；旧授权不升级、撤卡片连带撤正文 | PS-05B-1；合同见 [025 对话文本只读](PROJECT_TASK_CONTENT_READ_CONTRACT.md) | 实现 `5baec0d` 经 GLM 固定快照只读代码审查 GO、无 P0/P1；Codex 独立复测后端项目域 356 通过、4 项 Windows POSIX 专项跳过，前端项目页 134/134，三身份 API 和真实登录 Chrome 关键链路通过。该实现已推送用户仓库的 `feature/project-task-text-025`，随后与本台账一起快进 `main`；真实 PG 实库和 GLM 指出的 P2 锁序/归档竞态待验。该片不含附件、外链、执行流或协同写入，见 [025 验收记录](PROJECT_TASK_TEXT_025_ACCEPTANCE.md)。 |
| PS-05B | 显式任务分享/撤销和任务级协作者 ACL；逐个覆盖历史、搜索、附件、文件、导出、WebSocket 与后台入口 | PS-05A；任务目录访问范围设计 | 被分享者仅读被授权任务；撤销后旧深链、下载 URL、流订阅和后台任务入口失效；项目成员无默认读取权。 |
| PS-05C/PS-07A-1 | 028：成员预览项目指令并确认摘要，单专家新建私密任务；同事务创建对话/历史投影/项目关联/不可变指令快照，每轮按有效成员关系注入，旧摘要 409 | 021 私密任务、024/025 指定成员授权及 019 项目成员；合同见 [028 创建与快照](PROJECT_TASK_CREATE_CONTEXT_028_CONTRACT.md) | Codex 独立定向前端 70/70、后端首轮 45/45、迁移与相邻 42/42，隔离三身份 HTTP 与真实 Chrome 登录/创建/跳转通过；扩大回归、固定 SHA 的 GLM 只读审查和推送见[028 验收](PROJECT_TASK_CONTEXT_028_ACCEPTANCE.md)。仅支持单专家，未发送真实模型轮；PG、WorkBuddy 全状态视觉、协同/移交、项目目录和公共凭证仍待验。 |
| PS-07A-2 | 029：owner/admin 管理有序的全局共享单专家名单；非空名单以修订号门禁约束后续新项目私密任务，成员仍需自己的 Agent ACL；只读成员显示不可用占位 | 028 新建私密任务/指令快照、019 成员角色；合同见 [029 专家选择](PROJECT_EXPERT_SELECTION_029_CONTRACT.md) | `3fbc9f0c` 初版及 `b6f51b32`、`9d7c98bc`、`64ab31be`、`7ca5064c` 修复均经 GLM 固定源码审查 GO、无 P0/P1；远端 `main=7ca5064c` 已核对。Codex 独立受影响后端 107、前端项目/API 209、最后草稿/刷新 18 通过；先前四身份 HTTP、owner 浏览器名单保存与 390px 弹窗通过，详见[029 验收](PROJECT_EXPERT_SELECTION_029_ACCEPTANCE.md)。PG 实库、真实模型轮和 WorkBuddy 逐状态视觉未验。技能/连接器/定时任务、团队公共凭证仍属 PS-07A 后继。 |
| PS-08A · 030 | Windows/Linux 可用的任务级受控文件工作区；只开放文件工具，shell/终端、云端与自选主机目录另片 | 028/029 项目任务与专家门禁；[030A 第三版合同](PROJECT_TASK_WORKSPACE_030_CONTRACT.md) | GLM 源码审计证实当前后端按 Agent 而非任务隔离；首两版设计均 NO-GO。用户同意建立承接计划后，GLM 在固定 `32610c59` 上对第三版给出**设计 GO、无 P0/P1**，只认可 M0 优先顺序，未运行测试。[M0 工具/路径探针](PROJECT_TASK_WORKSPACE_030_M0_ACCEPTANCE.md)在 `96dc290a` 经 Codex 独立 Windows/WSL 复测通过；尚未接入运行链路，030A 功能未实现、未验收，不核销 PS-08。 |
| PS-07A | 项目级指令、专家、技能、连接器、定时规则的角色编辑与版本快照；连接器明确区分成员各自的个人授权与另行建立的团队公共凭证 | 019 成员/角色；调用路径安全审计 | 配置刷新持久，任务只注入启动时有效快照；未建立公共凭证时绝不用其他成员的个人 OAuth。公共凭证的创建、使用、撤销和审计须有独立权限测试。 |
| PS-05C | 项目任务新建、协同对话、队列/移交和任务引用待办/资产 | PS-05B、PS-06A、PS-07A、任务工作目录 | owner/member/撤权者跨刷新协同；移交前后执行者与配置/资源权限一致，任务状态不出现伪完成。 |
| PS-08 | 本地/云端项目任务入口与真正执行环境 | PS-05C，任务目录沙箱；云端资源能力另验 | 本地路径规范化与符号链接逃逸拒绝；云端无法读取本机文件；两个入口均能启动/恢复任务并正确报告失败。 |
| PS-09 | 项目归档/恢复、资源保留和可读审计 | PS-03A、PS-06A、PS-05B | 多身份验证归档后入口和资源策略；并发失败可恢复；审计只保留安全摘要。 |
| PS-10 | 项目外部数据源：定时导入与 Webhook | PS-06A、PS-07A、PS-09 | 签名/重放保护、来源标识、失败重试、撤权停用与凭据隔离；无真实外部源时保留实接验收缺口。 |
| PS-11 | 项目邀请/审批/动态/协作的消息中心 | 019 邀请、PS-03A 动态、PS-05B 协作 | 未读数/处理/深链与刷新一致；被撤权者不再从通知预览看到项目或私密任务。 |
| UI/交付 | 固定视窗逐状态视觉、键盘/窄屏/中英双语、Windows 安装与全量回归 | 各适用批次的真实数据和接口 | 熊宝品牌三栏与项目四页签按 WorkBuddy 参考逐状态截图；每个控件有真实行为或明确禁用理由，远端 SHA 与验收记录一致。 |

# Validation strategy

2026-09-24 进度注记：001/003/004 已在 Agent Orchestrator 台账通过并推送；002 的项目 UI 已由 Codex 整合推送，但 OpenCode 原任务未交付可接受差异，台账保持进行中。005 的 Claude 020 后端候选越出工作路径白名单，Codex 仅取所需文件并修正；OpenCode 前端候选在允许范围内交付，Codex 修复后通过定向测试、生产构建和隔离浏览器关键链路，GLM 只读审查指出的锁序与台账问题已修复，调度器标记该**实施片**完成。PS-04 的全部真实界面路径、PG 实库及 WorkBuddy 视觉仍未验收；不能凭复选框推断项目空间 1:1。

021 子计划 `plan-20260923-202541-d7404c` 的 Claude 后端与 OpenCode 前端候选由 Codex 合入 `a14e6ac8`；Codex 补了仓储层外人已关联任务泄漏回归、旧迁移版本断言与路由顺序测试，受影响后端 93/93、三身份隔离浏览器关键链路通过。GLM 对固定提交只读审查未发现已审代码中的越权泄漏，指出 PG 实库证据缺口与前端 409/解绑 404 提示问题；Codex 补 RED 回归修复，前端定向 35/35，推送 `351c4e64`。两代理和 GLM 自身都没有运行可采信的测试。该子计划只覆盖 PS-05A，不核销总计划 006，也不代表项目空间 1:1。

022 子计划 `plan-20260923-223519-8afe11`：GLM 首先只读审查 PS-03A 合同，Codex 修复跨数据库 JSON/TEXT 比较与无效游标错误码；Claude 后端与 OpenCode 前端并行交付候选，Codex 以失败测试修复跨项目留言正文串读、读请求撤权竞态、前端 404 残留和旧项目晚到 POST。固定首提交 `aeb8e01d` 的后端定向 44/44、既有项目相关 228/228、迁移发现 2/2，前端项目相关 72/72，类型/格式检查及生产构建通过；隔离三身份浏览器关键链路也通过。GLM 对该固定提交只读审查无 P0/P1，提出五项 P2；Codex 在 `6e4a216e` 修复审批入组动态、OpenAPI 响应模型、前端撤权竞态及过时文案，接受 SQLite 为保证撤权串行化的短时写锁性能取舍。修复后受影响后端 132/132、前端 73/73、类型/格式/构建与三身份浏览器关键链路再次通过。PostgreSQL 实库、完整全量套件、Windows 安装包与 WorkBuddy 视觉逐状态仍未验证。该子计划只覆盖 PS-03A，不核销总计划 008。

023A 子计划 `plan-20260924-010750-ec1755`：GLM 对私有项目资产初稿只读审查给出 NO-GO，指出“DB 先提交、磁盘后改名”会产生可见无文件的幽灵资产；Codex 将合同改为私有临时文件 → 原子改名 → 同事务成员锁/父节点校验/元数据提交，并明确 W1–W4 崩溃窗口、24h 受限孤儿回收、`is_current` 与复合外键/部分唯一索引。Claude 后端与 OpenCode 前端按冻结合同和不重叠路径交付，Codex 修正 Unicode 名称键、迁移版本断言和私有权限/下载 no-follow 流，将首个集成快照固定为 `a58d7b36`。GLM 对该快照源码只读审查 GO、无 P0/P1；Codex 以失败测试修复其可执行 P2：Unicode 搜索/超长查询、下载中间目录替换、清理器摘要及测试覆盖。修复后资产定向 99 通过、5 项 Windows POSIX 专项跳过，既有项目域 203 通过，前端项目相关 98 通过，类型/格式/构建及隔离三身份浏览器上传/下载通过；WSL 独立探针验证 0700/0600 与最终/中间目录符号链接替换拒绝。GLM 未运行测试，PG 实库未验。023A 仅是 PS-06 第一片，版本历史/恢复/删除、任务引用与 WorkBuddy 视觉另外验。

PS-05B 前置安全修复子计划 `plan-20260924-040719-701cfd`：OpenCode/DeepSeek V4.1 Flash 将 Dashboard HITL 恢复请求的 thread 归属检查置于 pending 查询、策略写入和 SSE 生成之前，固定提交 `a0d43f5e`。Codex 定向 **10/10**、相邻聊天/HITL/WS **51/51** 及类型/格式检查通过；GLM 对固定快照只读审查 GO、无 P0/P1。它只封闭一个既有入口，不提供项目任务显式分享或协同。下一批 PS-05B-1 应先固定任务协作者 ACL 合同，再分离任务级读取面与 Agent 级工作区/文件入口；WS 订阅撤权和个人凭据边界不得被“项目成员”身份绕过。

024 子计划 `plan-20260924-064432-5af730`：GLM 首先只读挑战 PS-05B-1 合同，Codex 修复其两项 P1 后冻结；Claude/Qwen3.8 Max 后端候选因临时 `.tmp` 文件越出白名单而未获执行任务验收，Codex 只整合允许路径并补接线、DTO 与测试；OpenCode/DeepSeek V4.1 Flash 前端候选经 Codex 修正测试选择器后独立验收通过。`e98cea4f` 的后端相关 74/74、前端 41/41、格式/类型/生产构建与隔离 1280×768 三身份浏览器关键链路通过，并已快进推送到用户仓库。GLM 对该固定提交源码只读审查 GO、无 P0/P1，列出四项 P2。其后 Codex 修复两条受 024 影响的旧迁移测试，OpenCode 在第三次精确白名单任务中补了真实双连接 SQLite 交错写测试；Codex 复测项目/邀请后端 312 通过、4 项 Windows POSIX 专项跳过，前端项目页 118/118，交错写独立重复 5 次通过。真实 PG 实库仍未验。此批只分享安全卡片摘要，WorkBuddy 对话分享面板及正文/附件/执行流访问仍归后续 PS-05B-2/3，不核销 PS-05B 总项或项目空间 1:1。

每个实施片由 Codex 检查完整 diff 与路径白名单、独立重跑定向测试、类型/lint/构建；根目录 `make all` 是最终 ship bar。首次安全批次验收用 owner、member、outsider 三身份分别验证列表、详情、改名和成员页，刷新后数据一致。后续逐项跑 [PROJECT_SPACE_SPEC.md](PROJECT_SPACE_SPEC.md) 的 PS-01–11。GLM 只读审查不能替代 Codex 复测。

官方 5.0.0 更新日志补充确认了外部数据源定时导入/Webhook 与个人消息中心。当前 Agent Orchestrator 八项快照在此发现前创建；以下是后继批次的显式待派工，**不是已实现或当前八项快照的完成证据**：Claude 负责数据源凭据、签名/重放保护、调度和失败重试；OpenCode 负责项目数据源配置与消息中心 UI；GLM 审查越权与事件泄漏；Codex 整合并以 PS-10/11 验收。后继批次需绑定新的持久计划项，不能仅在 008 中口头核销。

# Completion criteria

每批已接受变更推送用户仓库后记录远端 SHA；全部旅程和视觉证据齐全或明确保留项后才可声明项目空间对齐。
