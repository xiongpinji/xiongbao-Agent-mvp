# PS-05B-2A · 项目任务对话文本只读验收记录

日期：2026-09-25。实现提交：`5baec0de3445096601b805fd9da30038dc64e89e`。范围仅是任务本人对已经获授 024 卡片摘要的**当前项目成员**再次显式授予安全纯文本只读；授予、撤回与历史任务读取的边界见 [冻结合同](PROJECT_TASK_CONTENT_READ_CONTRACT.md)。本记录是该实施片的证据，不代表项目空间或 WorkBuddy 功能/UI 已 1:1 对齐。

## 可验证行为

- 025 配对 SQLite/PostgreSQL 迁移新增独立文本授予表，不回填 024 旧卡片接收者。项目成员、管理员或项目 owner 身份本身不能读取其他人的私聊正文。任务本人只能对有效卡片接收者授予；撤卡片在同一事务内删除文本授权，重授卡片也不恢复正文权限；移除成员后旧入口 404。
- 读取只投影已完成历史中的 human/assistant 文本；工具调用、流错误、其他消息类型与超限行不进入响应。翻页游标按原始序号推进，避免过滤行让翻页停滞。私有附件、工具参数、文件路径和原始 JSON 不在此响应中。
- 前端对本人提供“可读文本 / 撤回文本”，对接收者仅提供只读纯文本；撤权/切换项目后清理旧正文。仍没有项目内完整对话、附件、执行流、协同写入、微信/链接/二维码分享、任务新建与本地/云端执行。

## 独立验证与审查

| 证据 | 结果与边界 |
| --- | --- |
| 后端项目/资产/邀请/迁移回归 | **356 通过，4 项 Windows POSIX 专项跳过**；先前 025 定向 117/117。Ruff、Ruff format 和修改源文件的 strict mypy 通过。|
| 前端项目页/API 回归 | **134/134 通过**，其中 025 定向 52/52；TypeScript 构建、目标 ESLint/Prettier、Vite 生产构建通过。|
| 隔离 API 旅程 | owner/member/admin/outsider 的卡片授予、文本授予/撤回、分页、过滤、历史私密、成员移除、旧授权不升级通过；使用可控本地假 provider，不代表真实外部服务。|
| 真实登录 Chrome 旅程 | owner 在项目任务页明确授予/撤回，member 在“与我分享”只读查看；含脚本标签的正文以字面文本呈现，工具/错误行未显示。浏览器无页面错误；使用隔离测试数据。|
| GLM-5.3 固定快照只读审查 | 作业 `qwen-code-review-20260924-180342-1d6a98` 对 `5baec0d` 给出该后端片 **GO、无 P0/P1**；其独立工作树干净，GLM 未运行测试。第一次宽范围审查因会话轮数耗尽未形成报告，不计验收。|

## 未通过或未验证

- **PostgreSQL 实库迁移/并发未验**：本机 WSL 没有 PostgreSQL 工具，Windows Docker daemon 未运行。GLM 静态审查提出 P2：读取时多表 `FOR SHARE` 与解绑级联删除可能产生死锁，数据库会中止一方请求；归档与授予之间还存在短暂状态竞态。这两项在真实 PG 环境补并发测试及修复，不视为已消除。
- 全量 Dashboard Vitest 此次不是绿色：1214 个通过、4 个失败、3 个测试文件加载失败；失败位于本片以外的 DOMMatrix、专家发布、通道面板、输入动作与轨迹面板区域，是否为既有基线尚未证明。根目录 `make all` 未运行：Windows 环境没有 `make`。仓库 pre-commit hook 因同一缺失在提交前失败；Codex 手动完成上述相关检查后用 `SKIP_PRECOMMIT=1` 提交，不能把 hook 或最终 ship bar 标为通过。
- WorkBuddy 5.6.2 桌面观察显示完整项目任务过滤、项目内对话及分享面板；本片只完成其中安全纯文本读取的一段。1280×768 逐状态视觉与真实多端协作还未验收，见 [项目空间差距台账](PROJECT_SPACE_GAP_AUDIT.md)。
