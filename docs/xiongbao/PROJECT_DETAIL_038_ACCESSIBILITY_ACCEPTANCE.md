# 038 项目详情无障碍修复验收

2026-09-26。Claude/Qwen 在 037 基线 `83706b33` 上修复 035 审查留下的三项 P2；Codex 独立核对五文件差异并在固定候选 `3bb4eb43952888358e152bd466ec799d25937158` 运行测试。GLM-5.3 对该固定提交只读复核 **GO，无 P0/P1**。代码合入主线为 `7fe6e9ad35e2923da2428f553d08e767a6ddd733`。

项目信息披露从内部按 Escape 时仍收起并回焦触发器；焦点已移到外部控件时，Escape 收起而不抢走外部焦点。英文和中文资源包都为信息面板提供无障碍名称。12px 的项目指令展开按钮改用明暗主题共享的正文色，保留下划线、悬停和键盘焦点提示；静态令牌核对的明暗对比度分别为 17.74:1、13.73:1，覆盖现有品牌色盘。

候选定向 ProjectDetail **27/27**、Projects **221/221**，目标 ESLint/Prettier、TypeScript/Vite 构建和差异检查通过；合入后 Windows 主线 Projects **221/221**、`tsc -b` 与 Vite 生产构建通过。GLM 没有运行这些测试。构建保留既有大 chunk 提示。

GLM 另指出同开信息面板与编辑弹窗时一次 Escape 可能关闭两层、部分暗色品牌焦点轮廓的非文本对比偏低，以及全局双语键树缺少一致性检查；这些是后续 P2。移动端信息面板随页头滚动的 P2 仍开放。真实 WorkBuddy 逐状态视觉、桌面键盘及多用户旅程未由本修复核销，项目空间 11 条旅程的双重验收仍为 **0/11**；底部项目任务输入仍禁用。
