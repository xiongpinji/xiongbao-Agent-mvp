# 039 项目模板弹窗稳健性验收

2026-09-26。OpenCode/DeepSeek 在 037 基线 `83706b33` 上修复项目名无障碍提示与弹窗会话边界；Codex 独立检查、验证固定候选 `10b2a8dcc8f1c952431f97c2a1a6aea6b582698d`，GLM-5.3 只读审查 **GO，无 P0/P1**。合入主线为 `a8ca97c144046cc9a13074b6181e8ec6df1ca943`。

项目名输入的必填与无效状态现在关联固定的帮助/错误文本。编辑弹窗保持开启时若目标项目 ID 改变，会请求关闭；即使父级未及时关闭，提交前的 ID 校验也阻止把旧草稿 PATCH 到新项目。同一 ID 的服务端刷新保留当前草稿。模板覆盖确认只在创建它的开闭会话有效，关闭再打开后旧确认不会覆盖新草稿。原有普通新建、模板预填、失败重试和编辑载荷不变。

OpenCode 报告三项新增回归在实现前为红；Codex 在隔离候选复跑 ProjectSpace **27/27**、Projects **223/223**，目标 ESLint/Prettier、TypeScript 与 Vite 构建、差异检查通过。与 038 合入后，Windows 主线 Projects **225/225**、`tsc -b` 与 Vite 生产构建通过；构建仍有既有大 chunk 提示。GLM 没有运行测试，真实桌面/多用户浏览器旅程未由本批验证。

GLM 留下三项非阻塞 P2：组件重挂载时旧确认令牌可能碰撞、会话关闭时悬挂的确认框未主动销毁、动态错误文本没有自动播报。此修复不开放项目资源授权或受控文件任务，也不核销 PS-01 的完整搜索/创建/编辑/刷新旅程与视觉对照；项目空间双重验收仍为 **0/11**。
