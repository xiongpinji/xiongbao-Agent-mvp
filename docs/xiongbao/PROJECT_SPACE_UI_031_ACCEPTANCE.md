# 项目空间 031：四标签常驻输入区候选验收

2026-09-26。此片只调整项目详情的布局与提示，不开放项目页直接发送、协同写入或本地/云端执行。对标依据是 [WorkBuddy 实机观察](WORKBUDDY_LIVE_UI_AUDIT.md)：5.6.2 项目页的动态、计划、任务、资产共享底部输入区和右侧项目配置。

## 已交付边界

- 四个标签始终只有一个项目输入区；文本框和发送按钮保持禁用，清楚说明需要先在“任务”页新建任务并进入对话。
- 桌面工作区的标签内容独立滚动，输入区位于其下方；300 px 项目配置列常驻右侧并独立滚动。移动端保持纵向排列。
- 非成员 404 和加载失败状态不显示项目输入区或项目配置。

## 独立验证

- Codex 在隔离工作树复核完整三文件差异：`ProjectDetail.tsx`、对应测试和新增样式；定向 Vitest **9/9**、TypeScript、定向 ESLint、Prettier、`git diff --check`、生产构建通过。
- 使用隔离合成账号/项目、真实 Chrome 渲染已构建前端，在 **1273×634、1280×768、800×728、390×844** 四种视口逐一切换四标签，检查单一禁用输入区、配置可见、桌面内容独立滚动与移动端无横向溢出。800×728 预设现有 56 px 导航栏时，输入框实测 308 px、配置列 300 px、页面滚动宽度 800 px；截图保存在工作区 `outputs/project-space-031/800x728-collapsed-nav.png`。
- 复制到主仓后，Windows 环境再次运行该测试文件，**9/9** 通过。

## 未核销

031 没有自动收起窄窗口的全局导航；该事项由 032 单独处理，在此之前 800 px 默认展开导航时中栏仍过窄。031 也没有消除项目标题/卡片容器与 WorkBuddy 工作台的视觉差异。合成浏览器验证不能证明生产登录、真实项目消息发送、工具/文件权限或任何项目空间 PS-01–11 用户旅程完成；当前双重验收仍为 **0/11**。
